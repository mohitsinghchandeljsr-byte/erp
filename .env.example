# Database Configuration
# Replace with your actual PostgreSQL connection string
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# For local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/gaya_erp

# For Neon (recommended):
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# PostgreSQL connection details (auto-populated from DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=gaya_erp

# Session Secret
# IMPORTANT: Generate a strong random string for production
# You can use: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-string-in-production

# OpenAI API Key (Optional - for AI Chatbot feature)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Object Storage (Optional - for file uploads)
# These are auto-configured by Replit
DEFAULT_OBJECT_STORAGE_BUCKET_ID=
PUBLIC_OBJECT_SEARCH_PATHS=
PRIVATE_OBJECT_DIR=

# Application Settings
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS if needed)
FRONTEND_URL=http://localhost:5000
